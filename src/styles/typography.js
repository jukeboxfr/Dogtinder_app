import { StyleSheet } from 'react-native';
import Colors from "./colors";

const title = {
  fontSize  : 20,
  fontWeight: "bold",
  color       : Colors.grey,
  textAlign   : "center",
  marginBottom: 20
}

export default StyleSheet.create({
  title
})
