import { StyleSheet } from 'react-native';
import Colors from "./colors";

export default StyleSheet.create({
  "title": {
    "fontSize": 20,
    "color": Colors.grey,
    "fontWeight": "bold",
    "textAlign": "center",
    "marginBottom": 20
  }
})
